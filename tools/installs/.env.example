# API key to authenticate claude gateway apis
GATEWAY_API_KEY=
# Secret key to authenticate requests to the getpod-manager API
API_KEY=
# Home directory of the VM user (used for SSH keys and code-server config)
USER_HOME=/home/ubuntu
